/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.{js,ts,jsx,tsx,mdx}"],
  theme: {
    extend: {
      colors: {
        'main': '#FF4D30',
        'primary': '#', // replace with your main color
        // 'secondary': '#b44949', // replace with your main color
      },
    },
  },
  plugins: [],
}

